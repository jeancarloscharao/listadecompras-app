import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'io.ionic.listadecompras',
    appName: 'Lista de Compras',
    webDir: 'www'
};

export default config;
